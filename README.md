# 币安公开带单监控

只读取两名币安带单员公开展示的数据，用于 Gate USDT 永续合约的纸面模拟。

- 不登录币安或 Gate
- 不使用交易 API Key
- 不提交订单或点击跟单
- 不代表真实成交

GitHub Actions 云端连通性测试已通过。完整监控已启用，使用标准 `*/5 * * * *` 计划约每5分钟运行一次；平台繁忙时可能延迟。检测到开仓、加仓、减仓、平仓或反手等操作变化时立即发送详细通知；连续约60分钟未检测到操作时发送一次“无操作”。若公开数据读取失败，则发送“无法确认”，不会误报无操作。

PushPlus 同时发送到群组 `binancecopy97` 和 `binancecopy98`。
